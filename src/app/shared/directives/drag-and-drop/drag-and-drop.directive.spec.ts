import { DranAndDropDirective } from './drag-and-drop.directive';

describe('DranAndDropDirective', () => {
  it('should create an instance', () => {
    const directive = new DranAndDropDirective();
    expect(directive).toBeTruthy();
  });
});
